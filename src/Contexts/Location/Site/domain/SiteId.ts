import { Uuid } from '../../../Shared/domain/value-object/Uuid'

/**
 * Represents the unique identifier for a Site.
 * It extends Uuid, implying its value is a UUID string.
 */
export class SiteId extends Uuid {}
