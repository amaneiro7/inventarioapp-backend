import { Uuid } from '../../../../Shared/domain/value-object/Uuid'

/**
 * @class DeviceId
 * @extends Uuid
 * @description Represents the Value Object for a Device's unique identifier (UUID).
 */
export class DeviceId extends Uuid {}
